# Gemini API key used by the Vite dev server proxy.
# NEVER commit your real .env file. This key must NOT be exposed to the client
# bundle in production - deploy a serverless/edge function as the proxy instead.
GEMINI_API_KEY=your_gemini_api_key_here
